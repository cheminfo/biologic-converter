/**
 * Depending on the technique (shortname)
 * set the x and y
 * This function should be used for both MPR and MPT
 * it would consist in searching a particular label through the keys
 * and then either assign a good val to "x" or swap with another key to "x".
export function selectSuitableXY(){
// if "desired" is !== from x
// if x is not designed assign to x
// if x is assigned (and desired is not x) swap with x
}
*/
